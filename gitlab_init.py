URL = input("Enter the URL for the GitLab instance (default: http://localhost:8980)")
URL = URL if URL else "http://localhost:8980"
PROJECT_NAME = input("Enter the project name (default: docker-fastapi)")
PROJECT_NAME = PROJECT_NAME if PROJECT_NAME else "docker-fastapi"
